# @bufferapp/publish-app-shell

This package is the AppShell that wraps the Publish UI. It is a High Order Component that wraps the AppShell from `@bufferapp/ui` and configures it with the right menu items.
